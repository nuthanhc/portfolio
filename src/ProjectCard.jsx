import React from 'react';
import './Main.css'; // Import your CSS file for styling

const ProjectCard = ({ imageSrc, title, onClick }) => {
  return (
    <div></div>
  );
};

export default ProjectCard;
